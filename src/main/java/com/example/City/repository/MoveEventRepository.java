package com.example.City.repository;

import com.example.City.model.MoveEvent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MoveEventRepository extends JpaRepository<MoveEvent, Integer> {

}
